# 技术手册缺项参数与算法说明

本文补充技术手册中检索与遗忘章节尚未展开的参数、公式和算法流程。文中参数以当前仓库稳定版本为准。由于各阶段承担的任务不同，候选提出、标签判别、记忆排序和遗忘维护分别使用独立分数；不同阶段的分数不能直接横向比较。

## 1. 实施状态说明

| 模块 | 当前状态 | 用途 |
| --- | --- | --- |
| 四主标签匹配与 Observation 组装 | 主链路启用 | 从输入中识别条件、时效、态度和对象 |
| 结构化向量候选召回 | 主链路启用 | 召回候选记忆并进行精排 |
| BM25 与麒麟 Embedding 混合检索 | 严格检索器已实现；级联快速路径已完成独立评测 | 补充精确关键词检索并降低部分请求时延 |
| 高维稀疏随机投影 | 已接入开关，默认关闭 | 作为大规模候选生成器的预留实现 |
| Weibull 生命周期衰退 | 生命周期主链路启用 | 计算记忆稳定性并执行惰性遗忘 |
| 记忆数量压力归档 | 已实现规划器，默认不调用 | 根据时间和活跃记忆数量生成批量归档计划 |

其中，“默认关闭”表示代码和接口已经存在，但不会改变当前稳定链路的检索结果或记忆状态。

### 1.1 系统向量数据库 SDK 接入

当前主链路的向量数据库已切换为麒麟系统向量数据库 SDK。文本向量仍由麒麟 Embedding SDK 生成；向量写入、集合创建、近邻搜索、查询和删除由 `libkysdk-vector-engine-client` 通过系统 `kylin-ai-vector-engine` 服务完成。

旧的 Milvus-Lite 嵌入式数据库文件不删除、不迁移、不作为默认链路使用。若调试或回滚需要，可显式设置：

```text
NEX_AGENT_KYLIN_VECTOR_MODE=embedded
```

默认配置下，记忆库使用 `NEX_AGENT_KYLIN_VECTOR_MODE=sdk`，并通过 `LoadDBFile(db_file)` 将本地数据库文件加载到麒麟向量引擎。Python 侧只保留一个兼容适配层，上层 Observation、Retrieval、Lifecycle 的调用接口不需要改变。

## 2. 四主标签匹配方式、权重与阈值

### 2.1 标签定义

Observation 使用四类互补标签描述一条可形成记忆的信息：

| 标签 | 含义 | 示例 |
| --- | --- | --- |
| `condition` | 偏好适用的工作场景、任务环境或应用上下文 | 报价邮件、代码审查、在 Codex 中解释代码 |
| `temporal` | 偏好的相对有效期 | 当前会话、最近一段时间、以后统一 |
| `attitude` | 用户对目标的方向与强度 | 尽量避免、可以考虑、优先使用 |
| `object` | 态度直接作用的办公对象 | 柱状图、PDF、ChatGPT Codex、邮件模板 |

`condition` 是偏好成立的环境，`object` 是态度作用的主体。应用名称并不固定属于某一类：例如“在 Codex 中优先用表格解释”中，Codex 是 `condition`，表格是 `object`。

### 2.2 匹配流程

四标签匹配依次执行以下步骤：

1. 根据标点、中文空格和中英文语言切换产生软边界，并提出偏多保留的短语候选。
2. 对候选先执行规范标签及别名的精确匹配。
3. 对未精确命中的候选，使用麒麟 Embedding 与多组标签原型计算余弦相似度。
4. 同时与 `residual` 空标签比较，允许无信息位置保持为空。
5. 将候选映射到封闭的 `condition`、`object` 标签表；将态度映射为连续值，将时效映射为生命周期初始化值。
6. 以“态度 + 对象”为核心组装 Preference Frame，再附加距离合适的条件和时效。
7. 仅保留通过帧置信度和记忆形成门槛的结果。

每个语义标签由多个原型句表示。候选短语 (x) 对标签 (l) 的原型相似度为：

\[
s_l(x)=0.70\,s_{l,1}(x)+0.30\,\operatorname{mean}\left(s_{l,1}(x),s_{l,2}(x)\right)
\]

其中，(s_{l,1}) 和 (s_{l,2}) 是候选与该标签最相似的两个原型分数。该设计既保留最接近表达的主导作用，也要求候选得到同类第二个原型的支持，降低单一锚点偶然命中的影响。

角色假设分数为：

\[
H=s_l+0.55\left(s_l-s_{null}\right)
  +0.15\left(s_l-s_{second}\right)
  -0.003\max(0,L-10)
\]

其中，(s_{null}) 为残余文本相似度，(s_{second}) 为其他角色中最强竞争分数，(L) 为候选字符数。最后一项温和惩罚过长且混入无关信息的候选。

### 2.3 候选阶段阈值

候选阶段采用高召回门槛。这一门槛只决定“是否保留为后续假设”，不代表最终标签已经成立。

| 标签 | 最低相似度 | 最低空标签差值 (s_l-s_{null}) |
| --- | ---: | ---: |
| `condition` | 0.46 | -0.08 |
| `temporal` | 0.50 | -0.06 |
| `attitude` | 0.48 | -0.08 |
| `object` | 0.46 | -0.10 |

允许少量负的空标签差值，是为了避免在分句边界不完全干净时过早丢失有效短语。最终的规范标签匹配和帧组装会再次过滤。

### 2.4 规范标签匹配

`condition` 和 `object` 使用封闭标签表。对于非精确别名，默认最低相似度均为 0.54；每个角色假设最多保留两个规范标签。规范标签分数为：

\[
C=s+0.16I_{alias}+0.10\max(0,s-s_{second})
\]

其中，(I_{alias}=1) 表示命中规范标签或已登记别名。精确别名不受 0.54 相似度门槛限制。知识标签库可以扩充可匹配的场景与对象标签，但只提供标签候选，不把知识内容直接写成用户记忆。

### 2.5 态度值与时效值

态度使用七个近似正交的标尺点：

| 态度标签 | 数值 |
| --- | ---: |
| 强负向 | -1.00 |
| 负向 | -0.70 |
| 弱负向 | -0.35 |
| 不确定或中性 | 0.00 |
| 弱正向 | 0.35 |
| 正向 | 0.70 |
| 强正向 | 1.00 |

各锚点相似度通过温度 (T=0.08) 的 Softmax 转换为权重：

\[
p_i=\frac{\exp(s_i/T)}{\sum_j\exp(s_j/T)},\qquad
a=\operatorname{clip}\left(\sum_i p_i v_i,-1,1\right)
\]

时效标签不被解释为绝对截止日期，而是相对有效期的先验：

| 时效标签 | `promotion_seed` | 显式长期 |
| --- | ---: | --- |
| 短期 | 0.0 | 否 |
| 中期 | 1.0 | 否 |
| 长期 | 1.0 | 是 |

时效匹配默认要求相似度不低于 0.55，第一、第二候选差值不低于 0.02。时效置信度为：

\[
c_t=\operatorname{clip}\left(\frac{s_t-0.42}{0.45},0,1\right)
\]

### 2.6 帧组装及最终过滤

帧以态度和对象为必要核心。态度与对象的配对分数综合角色分数、规范标签分数、别名命中、区分度、结构位置和字符距离：

\[
\begin{aligned}
P={}&0.48H_a+0.17s_a+0.08c_a+0.20C_o+0.07H_o\\
 &+0.08\max(0,\Delta_o)+0.16I_{alias}+B_{structure}-0.012d
\end{aligned}
\]

默认要求态度相似度不低于 0.46、态度假设分数不低于 0.58。组装后的帧置信度为：

\[
\begin{aligned}
F={}&0.42s_a+0.38C_o+0.12C_c+0.08c_t+0.14I_{alias}\\
 &+0.12\min\left(1,\frac{\max(0,\Delta_o)}{0.12}\right)+B_{semantic}
\end{aligned}
\]

缺失 `condition` 或 `temporal` 时，对应项取中性值 0.5，而不是强行填充。组件默认帧门槛为 0.78；530 条在线检索评测采用更保守的 0.82。两者应在手册中分别标为“组件默认值”和“该次评测配置”，不可混用。

## 3. 标签权重 \(w_i\) 与过滤阈值 \(\tau\)

检索阶段将已知结构化标签按有效权重归一化。设查询与记忆在第 (i) 个字段上的匹配分数为 (m_i\)，则：

\[
S_{structured}=\frac{\sum_{i\in K}w_i m_i}{\sum_{i\in K}w_i}
\]

其中 (K) 只包含查询中实际已知的字段。字段完全一致时 (m_i=1)，一方包含另一方时 (m_i=0.6)，否则为 0。

| 结构化字段 | 权重 \(w_i\) |
| --- | ---: |
| 记忆类别 `category` | 0.35 |
| 记忆类型 `memory_type` | 0.15 |
| 场景 `scene` | 0.15 |
| 应用 `app` | 0.10 |
| 任务类型 `task_type` | 0.25 |

主检索排序分数为：

\[
S_{retrieve}=f_{status}\left(0.56S_{structured}+0.18S_{lexical}+0.26S_{semantic}\right)
\]

其中，词面分数使用查询词集合与记忆词集合的 Jaccard 相似度；语义分数由候选召回分数在当前候选集合内归一化得到。正常状态 (f_{status}=1)；非历史查询中的过时记忆取 0.72。

本阶段没有单一的全局过滤阈值 \(\tau\)。过滤分三类执行：

| 过滤位置 | 阈值或规则 |
| --- | --- |
| 硬过滤 | 用户必须一致；排除 blocked、deleted、archive；排除未来记忆；非历史查询排除不适用的历史记录 |
| Observation 标签过滤 | 使用第 2 节各角色独立阈值 |
| 候选数量过滤 | 后端默认召回 50 条，精排后默认返回 Top-5 |

这种分层设计比统一 \(\tau\) 更适合本系统：安全和用户隔离使用不可补偿的硬条件，语义不确定性使用连续分数，输出规模由 Top-k 控制。

## 4. 候选召回：多通道融合、排序参数与性能

### 4.1 现行候选召回

现行流程先由向量后端召回最多 50 条候选，再在原字段空间复核结构标签、词面重合、语义分数、有效时间和状态。候选生成负责缩小搜索空间，最终关系判断仍在原字段空间完成。

严格检索器还提供以下激活分数：

\[
A=0.25S_{condition}+0.20S_{slot}+0.20S_{goal}
 +0.25S_{semantic}+0.10S_{recency}
\]

时间新鲜度采用 30 天半衰期：

\[
S_{recency}=\exp\left(-\ln 2\cdot\frac{age_{days}}{30}\right)
\]

### 4.2 BM25 与 Embedding 通道

BM25 用于识别应用名、文件名、命令、错误码和固定术语等精确词面线索。其公式为：

\[
BM25(q,d)=\sum_{t\in q}IDF(t)\cdot
\frac{f(t,d)(k_1+1)}{f(t,d)+k_1\left(1-b+b\frac{|d|}{avgdl}\right)}
\]

默认参数为 (k_1=1.50\)、(b=0.75\)。BM25 分数归一化后与麒麟 Embedding 融合：

\[
S_{hybrid}=0.60S_{BM25}+0.40S_{Kylin}
\]

BM25 不负责理解用户意图；它先快速找到包含关键字的片段，Embedding 再判断这些片段是否与查询语义一致。

### 4.3 级联快速路径

已评测的级联方案为“BM25 Top-8 -> 麒麟 Embedding 重排 -> 保守提前返回”。单一上下文、无显式歧义、非多任务且不存在词面条件冲突时，候选综合分数为：

\[
S_{cascade}=0.72S_{semantic}+0.18S_{BM25\_ratio}
 +0.10S_{identifier}
\]

提前返回要求：语义分数不低于 0.75，综合分数不低于 0.82，第一与第二候选差值不低于 0.10。门槛只在训练分区校准，目标为直接返回精度不低于 99%；开发、测试和挑战分区只用于评估。

在 47 条记忆、530 条查询的固定评测中：

| 指标 | 原链路 | BM25-Embedding 级联 |
| --- | ---: | ---: |
| 必需记忆召回率 | 88.10% | 93.33% |
| 已返回记忆精度 | 93.59% | 94.69% |
| 全部必需记忆均命中的查询比例 | 86.96% | 94.13% |
| 澄清型查询正确 abstention | 100.00% | 100.00% |
| 平均总时延 | 508.90 ms | 402.58 ms（串行保守估算） |

245/530 条查询走快速路径，覆盖率为 46.23%，该批快速返回精度为 100%。快速路径自身平均 118.01 ms，P95 为 359.17 ms。冷启动时构建 BM25 和 94 个记忆片段向量约 19.66 s；该成本应在服务启动阶段完成，不计入热请求。

上述数据证明级联路径在该固定集合上有效，但样本中的记忆规模只有 47 条，不能替代大规模全局候选池的压力测试。

### 4.4 高维稀疏随机投影预留通道

系统保留 CountSketch 风格的高维稀疏投影候选器。每个原始特征通过哈希函数映射到唯一桶，并由独立符号函数决定正负方向：

\[
Y=XR,\qquad
R_{j\ell}=\begin{cases}
\sigma(j), & h(j)=\ell\\
0, & h(j)\ne\ell
\end{cases}
\]

默认参数为 128 个桶、3 次独立草图、随机种子 17、候选数 50、过采样倍数 4。多次草图结果用于降低哈希碰撞波动，召回后仍必须回到原始字段空间复核。

该通道当前 `sparse_projection_enabled=false`，仅保证接口已接入，不应在手册中宣称已有正式性能收益。

## 5. 混合检索参数、排序开关与超时降级

### 5.1 参数表

| 参数 | 默认值 | 说明 |
| --- | ---: | --- |
| `candidate_limit` | 50 | 混合精排前最大候选数 |
| `bm25_k1` | 1.50 | 词频饱和参数 |
| `bm25_b` | 0.75 | 文档长度归一化参数 |
| `lexical_weight` | 0.60 | BM25 通道权重 |
| `kylin_semantic_weight` | 0.40 | 麒麟语义通道权重 |
| `require_kylin_semantic` | `true` | 正式混合路径要求语义复核 |
| `sparse_projection_enabled` | `false` | 随机投影候选器开关 |
| 最终返回数 | Top-5 | 主记忆最大返回数量 |

排序顺序为：硬过滤、候选生成、BM25/Embedding 混合评分、结构化激活评分、冲突与有效性复核、Top-k 截断。随机投影或 BM25 的候选命中都不能直接创建记忆关系，也不能绕过用户隔离和原字段复核。

### 5.2 500--800 ms 自适应降级

在线 Retrieval 的目标平均预算为 500 ms，800 ms 为硬停止点。时间预算如下：

| 阶段 | 预算 |
| --- | ---: |
| 输入安全检查 | 5 ms |
| 规范化与上下文 | 5 ms |
| Observation | 480 ms |
| 记忆查询 | 5 ms |
| 输出安全与组装 | 5 ms |
| 总目标 | 500 ms |
| 硬停止 | 800 ms |

Observation 根据已消耗时间逐步收紧候选：

| 时间区间 | 模式 | 新候选准入条件 |
| --- | --- | --- |
| (t<500\) ms | `full` | 保留正常候选 |
| (500\le t<605\) ms | `selective` | 优先级不低于 0.58，或同组候选至少出现 2 次 |
| (605\le t<710\) ms | `strict` | 优先级不低于 0.70，或同组至少 2 次且优先级不低于 0.62 |
| (710\le t<800\) ms | `finalize` | 不再产生新候选，只组装已完成结果 |
| (t\ge800\) ms | `expired` | 返回最近一次完整检查点 |

只有剩余时间足以覆盖“预计一次 Embedding 耗时的 1.55 倍 + 48 ms 收尾预算”时，才允许开始新的向量计算。预计耗时使用指数滑动平均更新：

\[
E_{new}=0.8E_{old}+0.2T_{observed}
\]

800 ms 到达后不再启动备用 LLM 或新的召回通道，而是返回已经完成且通过门槛的最好结果。该机制牺牲的是后续低优先级候选的完整性，不降低已经完成结果的安全过滤要求。

固定 530 条在线评测中，记忆查询本身平均 0.51 ms；总平均时延 508.90 ms，P95 为 756.53 ms，必需记忆召回率 88.10%，已返回记忆精度 93.59%，澄清型查询 abstention 为 100%。主要性能成本位于 Observation 的 Embedding，而不是 Top-5 记忆排序。

## 6. 记忆遗忘曲线、衰减参数、阈值与资源回收

### 6.1 当前启用的 Weibull 生命周期

记忆形成时，先根据时效标签得到参考保留天数：短期 21 天、中期 90 天、长期 365 天，未知时效取中性值 60 天。时效置信度用于在中性值与标签值之间做对数域插值：

\[
D_{blend}=\exp\left((1-c_t)\ln 60+c_t\ln D_{label}\right)
\]

最终保留尺度还会受到独立支持证据和形成强度的温和修正：

\[
D=\max\left(3,
D_{blend}\left[1+0.18\ln(1+\max(0,n_{support}-1))\right]
(0.75+0.45b)\right)
\]

其中 (b\in[0,1]\) 为记忆形成强度。初始稳定性为：

\[
S_0=\operatorname{clip}(0.55+0.40b,0,1)
\]

随时间的稳定性采用 Weibull 生存曲线：

\[
S(t)=S_{anchor}\exp\left[-\ln 2
\left(\frac{\Delta t}{D}\right)^{1.15}\right]
\]

因此 (D) 是当前锚点下的近似半衰周期，形状参数固定为 1.15。记忆被正确检索后会建立新锚点，并扩大保留尺度：

\[
D'=D\left[1+0.35m+0.65m(1-S)\right]
\]

\[
S'_{anchor}=S+(1-S)(0.55+0.30m)
\]

其中 (m\) 为本次查询与记忆的匹配分数。`supports` 和 `confirms` 关系也可小幅增强稳定性，但普通相关关系和日志共现不延长寿命。

### 6.2 遗忘阈值

| 参数 | 默认值 | 行为 |
| --- | ---: | --- |
| 正常遗忘阈值 | 0.15 | 低于该值时进入遗忘判定 |
| 绝对救援下限 | 0.05 | 低于该值立即标记 `forgotten` |
| 边缘救援匹配度 | 0.85 | 稳定性在 0.05--0.15 时，达到该匹配度可被本次查询救援 |
| 生命周期最低匹配度 | 0.45 | 低于该值的记忆不参与本次生命周期选择 |
| 最低选择分数 | 0.32 | 生命周期综合分数的最低返回门槛 |

查询时执行惰性遗忘；低频 `maintain` 任务也可以批量扫描活跃记忆。批量维护不执行查询救援，稳定性低于 0.15 即转为 `forgotten`。

`forgotten` 是软状态：记忆退出正常检索，但仍保留内容、来源指针和状态转换记录供审计。该操作减少活跃候选数量和索引工作集，但不会立即缩小 SQLite 主文件。

### 6.3 按时间和记忆数量归档的预留规划器

为应对大量记忆造成的检索压力，仓库另有异步归档规划器。它每次维护时同时考虑距离上次维护的时间和活跃记忆数量：

\[
T=1-\exp\left(-\frac{hours}{24}\right)
\]

\[
P=\left(\frac{N}{800}\right)^{1.25}T
\]

\[
\tau_{archive}=\min\left(1,0.30+0.025\max(0,P-1)\right)
\]

每条记忆的保留分数为：

\[
R=0.30C+0.30S+0.20A+0.15U+0.05Q
\]

其中，(C) 为 confidence，(S) 为 stability，(A=0.5^{age/30}) 为最近激活度，(U) 为支持证据饱和度，(Q) 为来源可靠度。记忆年龄至少 1 天且 (R<\tau_{archive}\) 时，规划器建议归档。它不是只淘汰固定数量的末位记忆，而是归档所有低于动态门槛的记忆。

| 压力归档参数 | 默认值 |
| --- | ---: |
| 参考活跃记忆数 | 800 |
| 硬容量告警值 | 1000 |
| 维护周期 | 24 h |
| 压力指数 | 1.25 |
| 基础归档阈值 | 0.30 |
| 压力阈值增益 | 0.025 |
| 激活半衰期 | 30 天 |
| 新记忆保护期 | 1 天 |

1000 条是监控告警值，不是强制截断值。当前规划器只生成可审计的归档计划，不修改存储，也未被稳定引擎调用，因此该机制尚不能被描述为“在线自动控制在 1000 条以内”。

### 6.4 资源回收边界

系统应区分三种操作：

| 操作 | 检索可见性 | 审计内容 | 物理空间 |
| --- | --- | --- | --- |
| `forgotten` / `archive` | 默认不可见 | 保留 | 通常不立即回收 |
| 授权忘记 | 不可见 | 保留最小删除证明 | 删除主记录、证据与可重建索引 |
| 数据库压缩或 WAL checkpoint | 不改变语义状态 | 不变 | 在安全条件满足后回收文件空间 |

因此，遗忘曲线和压力归档解决的是活跃候选规模与检索压力；SQLite、WAL、运行日志、缓存和投影索引的彻底清理属于授权忘记与存储维护流程，不能仅通过把状态改为 `forgotten` 完成。

## 7. 手册表格建议

技术手册可将上述参数拆成三张正式表：

1. “Observation 标签匹配参数表”：记录角色相似度、空标签差值、规范标签门槛和帧门槛。
2. “Retrieval 召回与排序参数表”：记录候选数、Top-k、BM25、Embedding、结构化权重和超时阶段。
3. “Memory Lifecycle 参数表”：记录时效参考天数、Weibull 形状参数、遗忘阈值、救援阈值及压力归档参数。

建议另设“实施状态”列。稳定启用参数可作为系统默认值；级联评测参数应标为“已验证、待正式接线”；随机投影和压力归档参数应标为“接口预留、默认关闭”。这样既能完整描述算法设计，也不会超出当前代码实际行为。
