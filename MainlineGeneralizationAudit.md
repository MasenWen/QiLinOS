# 记忆算法主线扩展测试与泛化审计

## 1. 测试目的

本轮不再沿用单一小样例反复调参，而是使用项目根目录中此前未作为主要调试对象的数据，检查已经打通的算法主线能否迁移到新用户、新任务结构、低信息密度日志和更长生命周期。

测试覆盖五条主线：

1. Observation：从结构化事件或自然语言中形成四类标签。
2. Episode：把时间和任务上连续的 Observation 组织成局部任务段。
3. Candidate / KG：过滤关系信号，并投影为 Observation、Episode 和 Memory 层联系。
4. Activation / Recession：查询激活、稳定性增长、置信度初始化、晋升和遗忘。
5. Reflection：使用真实 DeepSeek API 审查错误记忆和重复记忆。

## 2. 数据与防过拟合设计

新增主要测试数据包括：

| 数据集 | 规模 | 主要用途 |
| --- | ---: | --- |
| os_agent_memory_benchmark_v1 | 336 个事件、312 个查询、192 条记忆 | 完整生命周期与关系图 |
| agent_memory_challenge_v2 | 544 个留出事件 | 新用户结构化事件与 Episode |
| memory_test_data | 308 个留出事件 | 新用户结构化事件与 Episode |
| WebLINX | 1034 个低密度 episode、11499 个字段值 | 稀疏日志聚合 |

三个 OS Agent 数据集共提供 1188 个新增主要事件。测试按用户留出，Observation 原型只允许使用训练用户；完全相同的归一化文本只计一次。输入和标准答案均来自现有数据，不使用 LLM 生成测试内容。

`os_agent_test_data/raw_json` 的 47 个文件与 v3.1 文件名及 SHA256 完全相同，因此只作为重复性审计，不重复计入泛化成绩。

结构化事件和自然语言被分成两个测试轨道。前者检查接口能否无损接收已有 key，后者才检查算法面对新表达时的语义抽取能力。

## 3. 总体结果

| 主线 | 核心结果 | 当前判断 |
| --- | --- | --- |
| Observation：结构化接口 | 852/852 个事件的 10 个关键字段完整保留 | 稳定，但不代表语义推断能力 |
| Observation：留出自然语言 | 49 条中形成 4 条，形成召回率 8.16%；已形成对象精确率 100% | 精确但过度保守，是当前最大瓶颈 |
| Episode：会话内局部任务 | 合并精确率 100%，合并召回率 72.73%，完整任务率 94.74% | 可用，偏向宁拆勿并 |
| Episode：结构化留出集 | 两个数据集均为 100% | 结构 key 场景稳定，不能外推到开放文本 |
| Candidate / KG | Observation 和 Memory 关系精确率、召回率均为 100% | 关系投影可靠，开放关系发现未被覆盖 |
| Activation | 完整标签召回率 100%；缺少 condition 时为 77.27% | 生命周期逻辑强，但依赖 Observation |
| Recession | 12/12 遗忘、24/24 晋升正确 | 支持的状态流转稳定 |
| WebLINX 稀疏聚合 | 65.18% 字段实例匹配；44.10% episode 形成较完整证据 | 聚合有效，尚无最终记忆精确率标准 |
| Reflection 修正 | 常规支持/隐私样例 8/8；显式替代样例 0/2 | LLM 能识别替代，应用策略错误覆盖了结论 |
| Reflection 合并 | 2 个应合并与 6 个不应合并组全部正确 | 保守性良好，样本量仍小 |

## 4. Observation

### 4.1 结构化事件

新的日志接口直接接受数据中的时间、用户、会话、场景、应用、对象和动作字段。852 个留出事件全部通过 schema 检查，关键字段没有丢失或错位。

服务器平均耗时约 0.13 ms，P95 约 0.20 ms。这个结果说明接口层已经足够轻量可靠，但它只证明“已有结构不被破坏”，不证明算法能从未知文本中发现相同标签。

### 4.2 自然语言

自然语言轨道共保留 49 条去重、脱敏后的安全样例。Kylin Embedding 实际使用 `ensemble-embd_gte-base_uint8-text`，并保持既定门槛。

算法只形成 4 条 Observation：

- 形成召回率：8.16%。
- 放弃率：91.84%。
- 对象精确率：4/4，100%。
- condition 填充正确率：2/4；其余为空，没有填入错误场景。
- 完整 Observation 精确率：2/4，50%。
- 完整 Observation 召回率：2/49，4.08%。
- 已形成项综合质量：7.5/10。

单条平均耗时约 119.67 ms，P95 约 274.44 ms，49 条中 47 条低于 300 ms；首次模型和标签初始化约 21.22 秒。

这说明当前策略没有出现大量无中生有和对象错配，但对新表达、新场景目录和偏好更新的召回严重不足。它更像一个高精确率过滤器，还不能独立承担通用自然语言 Observation 入口。这里存在明显的表达目录依赖，也是本轮最直接的过拟合风险。

## 5. Episode

在 v1 数据的会话内局部任务标准上：

- 合并精确率为 100%，合并召回率为 72.73%。
- 边界精确率为 90.44%，边界召回率为 100%。
- 94.74% 的标准任务没有被拆散。
- 没有跨任务错误合并，但有 12 个任务被保守拆分。

去掉会话硬边界后，合并召回率不变，精确率下降到 42.11%，出现 36 个跨任务合并。因此会话和明确任务切换信号仍然重要。

如果用跨会话的长期任务 lineage 作为标准，召回率只有 11.27% 至 26.76%，72 条长期 lineage 全部存在拆分。这并不完全是 Episode 错误：当前 Episode 定义本来就是局部连续任务，跨时间的长期联系应由后续 KG 承担。

两个结构化留出集得到 100%，主要说明 session/task key 可以稳定组织事件。由于数据结构较整齐，这一结果不能证明开放文本边界也达到同等水平。

## 6. Candidate 与知识图谱

测试建立了 336 个 Observation、72 个 Episode、192 条 Memory 和 51 个关系信号。

- 48 条有效 Observation 关系全部保留，精确率和召回率均为 100%。
- 36 条有效 Memory 关系全部生成，精确率和召回率均为 100%。
- 无向关系矩阵保持对称。
- 低于门槛、跨用户和重复证据三类过滤均被实际触发。
- 服务器完整处理约 5.34 ms。

本轮所有有效关系都发生在同一 Episode 内，因此 Episode 间边没有正样例。更重要的是，标准关系信号由数据上游明确提供；这里验证的是过滤、去重、方向和分层投影，不是从开放内容中发现未知关系。Candidate 主体实现可以固定，但仍需要未来数据覆盖跨 Episode 聚合和开放关系召回。

## 7. Activation 与 Recession

在 144 条活跃记忆和 312 次查询上，完整标准标签轨道中的 264 次目标记忆检索全部命中，选择精确率为 100%，每次成功使用都正确提高了 stability。

移除 condition、只保留 object 与 attitude 后：

- 目标记忆召回率下降到 77.27%。
- 已选择记忆的语义精确率仍为 100%。
- `paused_diary_task`、`spreadsheet_fill`、`active_project_summary` 和 `office_app` 等对象出现重复漏检。

这说明 condition 不是装饰标签，而是当前检索消歧的重要组成部分。

置信度初始化符合来源差异：文本记忆平均 confidence 为 0.703，日志记忆为 0.299，相差 0.404。状态流转中 12 条应遗忘记忆和 24 条应晋升记忆全部正确。ARCHIVE 和 ERASE 不属于当前生命周期算法负责的动作，未混入得分。

完整回放约 34.40 ms，单查询平均约 0.083 ms，P95 约 0.141 ms。生命周期模块本身表现稳定，但端到端上限仍由 Observation 的标签质量决定。

## 8. WebLINX 稀疏日志

1034 个 episode 中，每个 episode 至少找到一种语义证据；17 个唯一字段值中匹配 10 个，按实例计的匹配率为 65.18%。456 个 episode 同时拥有 attitude 与 condition/object/temporal 证据，可形成较完整的候选框架，占 44.10%。

这支持“低密度事件单条信息不足，但 Episode 聚合后能恢复更多证据”的设计判断。不过数据没有可靠的最终记忆标准答案，因此这里只能评价证据形成率，不能声称形成记忆的精确率已经达到 65.18% 或 100%。

## 9. Reflection

Reflection 使用服务器 MCP Host 中同用途的 DeepSeek 配置，实际模型为 `deepseek-chat`。修正和合并各执行一次主审与一次复审，共 4 次 API 调用，约 27.75 秒、23614 个 token；4 个临时 skill 文件均在结束后删除。

常规支持、证据不足和隐私脱敏样例 8/8 判断正确。合并测试中，2 组同一执行过程被正确合并，6 组替代、条件共存或矛盾记忆均正确保持分离。

显式替代测试 0/2。值得注意的是，LLM 的理由已经正确指出新版目录替代旧版目录，应该把旧记忆视为过时；但确定性应用层因为长期记忆尚未达到 240 天，将 verdict 强制改回 `supported`。因此问题不在 DeepSeek 的语义判断，而在“年龄保护规则压过显式替代证据”的策略顺序，同时每条审查包也缺少稳定、显式的继任关系上下文。

Reflection 合并可以作为稳定的保守模块；修正模块在常规证据上可用，但显式 supersession 必须拥有高于时间保护的优先级后，才适合称为稳定版本。

## 10. 综合结论

系统主线已经真正打通：结构化事件能够形成 Observation，局部事件能够组成 Episode，关系能够进入分层图谱，记忆能够被查询激活、晋升和遗忘，Reflection 也能在真实 API 上完成审查与合并。

当前质量并不均匀：

1. **已经稳定**：结构化 Observation 接口、Candidate 的确定性关系投影、Activation/Recession 的状态流转和性能。
2. **可固定但有清晰边界**：会话内 Episode、WebLINX 稀疏聚合、Reflection 合并。
3. **尚不能视为泛化稳定**：未知自然语言 Observation，以及 Reflection 对显式替代记忆的最终裁决。

本轮没有发现“所有模块都只记住原测试集”的证据：新用户结构化事件、低密度 Web 日志和生命周期回放均能工作。但自然语言形成召回率只有 8.16%，明确暴露了标签目录和表达覆盖不足；若把几个结构化轨道的 100% 直接当成端到端正确率，就会高估系统能力。

下一轮最值得投入的不是改动已经稳定的生命周期公式，而是：

1. 扩大 Observation 的语义候选召回，同时继续保持当前低误填率。
2. 给 Reflection correction 显式提供继任/冲突邻居，并让确定的替代证据优先于年龄保护。
3. 增加真实跨 Episode 关系标准，验证 KG 是否能连接被局部 Episode 合理拆开的长期任务。

在这三项完成前，系统适合作为“结构化日志优先、自然语言高精确率补充”的稳定研究版本，不宜宣称为对任意自然语言都具有稳定召回的完整版本。
